import React from "react";
import { Header } from "../components/index";
const Main = () => {
  return (
    <main className="flex w-screen min-h-screen flex-col items-center justify-center bg-primary">
      <Header></Header>
    </main>
  );
};

export default Main;
